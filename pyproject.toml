[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--noconftest -q"

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.ruff.format]
quote-style = "double"

